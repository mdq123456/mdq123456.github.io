---
export interface Props {
  showInPrint?: boolean;
  className?: string;
}

const {showInPrint = false, className = ''} = Astro.props;

const printClasses = showInPrint
  ? 'print:relative print:z-auto print:shadow-none print:mb-4'
  : 'no-print';
---

<div
  class={`construction-banner fixed top-0 left-0 right-0 z-50 px-4 py-3 shadow-sm ${printClasses} ${className}`}
>
  <div class='max-w-6xl mx-auto flex items-center justify-center gap-3 text-sm'>
    <div class='icon flex-shrink-0'>
      <svg
        class='w-5 h-5 text-amber-600'
        fill='none'
        stroke='currentColor'
        viewBox='0 0 24 24'
      >
        <path
          stroke-linecap='round'
          stroke-linejoin='round'
          stroke-width='2'
          d='M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.732-.833-2.5 0L4.232 13.5c-.77.833.192 2.5 1.732 2.5z'
        ></path>
      </svg>
    </div>
    <span class='text-amber-800 font-medium'> 🚧 Página en construcción </span>
    <span class='text-amber-700 hidden sm:inline'>
      - Los datos mostrados son de ejemplo y pueden no reflejar información real
    </span>
  </div>
</div>

<style>
  /* Estilos para el banner de construcción */
  .construction-banner {
    background: linear-gradient(90deg, #fef3c7, #fde68a);
    border-left: 4px solid #f59e0b;
    animation: fadeInSlide 0.5s ease-out;
  }

  @keyframes fadeInSlide {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .construction-banner .icon {
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.7;
    }
  } /* Estilos específicos para impresión */
  @media print {
    .construction-banner {
      position: relative !important;
      background: #fef3c7 !important;
      border: 1px solid #f59e0b !important;
      margin-bottom: 1rem !important;
      padding: 0.5rem !important;
      box-shadow: none !important;
      font-size: 10px !important;
    }
  }
</style>
